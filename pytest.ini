[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers -p no:cacheprovider -p no:launch_testing -p no:launch_testing_ros
markers =
    slow: 标记运行较慢的测试
    integration: 需要 MySQL 等外部服务的集成测试
