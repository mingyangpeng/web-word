---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app.py]
autonomous: true
requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "User can input a verb and see real-time results without clicking a button"
    - "Search bar is sticky at the top with blur effect"
    - "Empty state shows welcome message when no verb is searched"
    - "Debounce delay of 300ms prevents rapid-fire search triggers"
  artifacts:
    - path: "src/app.py"
      provides: "Sticky search bar, real-time search, empty state display"
      min_lines: 60
  key_links:
    - from: "st.text_input"
      to: "session_state.verb_result"
      via: "on_change handler"
      pattern: "st.text_input.*on_change=.*search_verb"
---

<objective>
Redesign the verb search input area with Google-style sticky search bar and real-time search functionality.

Purpose: Improve search interaction by removing the "Generate" button and making the search experience more fluid and responsive, matching user expectations from modern search interfaces.

Output: Sticky search bar at top, empty state welcome message, real-time search with 300ms debounce, search results display in session_state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-verb-content/02-CONTEXT.md
@.planning/phases/02-verb-content/02-DISCUSSION-LOG.md
@src/design_system.py

# Phase 1 Design System Reference
Phase 1 created the following design system components that this plan must use:
- HEADER_H1, HEADER_H2, HEADER_H3, HEADER_H4: Typography constants
- SPACE_XS, SPACE_SM, SPACE_MD, SPACE_LG, SPACE_XL: Spacing constants
- get_card_style(): Card container styles
- get_input_field_style(): Input field styles
- get_button_primary_style(): Primary button styles
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sticky search bar with real-time search</name>
  <files>src/app.py</files>
  <read_first>
    - src/app.py (lines 33-49 for existing sticky CSS, lines 216-236 for current search implementation)
    - src/design_system.py (HEADER_H1, HEADER_H3, get_card_style, get_input_field_style, get_badge_style)
  </read_first>
  <action>
    1. Replace the existing sticky search bar CSS (lines 33-49) with new CSS for a Google-style search bar:
       - Add `.google-search-bar` class for styling
       - Use `position: sticky` with `top: 1rem` and `z-index: 1000`
       - Add backdrop-filter blur effect for modern glassmorphism
       - Use rounded corners (12px) and subtle shadow

    2. Create a new `search_verb()` function with 300ms debounce:
       - Accept `value` parameter from text_input on_change
       - Store verb in st.session_state.verb_result
       - Set st.session_state.show_result = True
       - Set st.session_state.current_debounce_start = timestamp

    3. Replace the search input section (lines 216-236) with:
       - Import `st` and datetime for debounce timing
       - Create a `verb_input` st.text_input with:
         - Label_visibility="collapsed"
         - Placeholder: "搜索动词 (例如：跑进来、冲进来、滑进来...)"
         - Value="跑进来" (default example)
         - on_change=search_verb function
       - Wrap in a card container using get_card_style()
       - Add welcome message below input when no result is shown:
         - H2 title: "🏃 动词释义优化器"
         - H3 subtitle: "基于运动事件类型学的中文动词释义优化"
         - Info box with 4 key features

    4. Use session_state flag `st.session_state.current_debounce_start` to track debounce timing

    5. Use `get_input_field_style()` for input field and `get_badge_style()` for info badges

    6. Ensure proper spacing using SPACE constants (SPACE_MD between input and welcome text)
  </action>
  <verify>
    <automated>grep -c "google-search-bar" src/app.py &gt;= 1</automated>
    <automated>grep -c "st.session_state.verb_result" src/app.py &gt;= 1</automated>
    <automated>grep -c "search_verb" src/app.py &gt;= 1</automated>
    <automated>grep -c "on_change=search_verb" src/app.py &gt;= 1</automated>
    <automated>python -c "import ast; code = open('src/app.py').read(); tree = ast.parse(code); print('Syntax OK')"</automated>
  </verify>
  <done>
    - Search bar is sticky at the top with blur effect
    - Real-time search triggers when user types (no button needed)
    - Empty state displays welcome message when no verb is searched
    - Debounce delay is 300ms
    - Default value is "跑进来" for demonstration
    - Proper spacing and styling using design system constants
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Search Function | User input flows through search_verb() function into session_state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | verb_input | mitigate | Validate verb length (min 1 char, max 50 chars) in search_verb function |
| T-02-02 | Tampering | st.session_state.verb_result | accept | No sensitive data stored, local session only |
</threat_model>

<verification>
[Phase 2 UI-01 Verification]
- Run Streamlit app: streamlit run src/app.py
- Verify search bar is sticky at top with blur effect on scroll
- Type a verb (e.g., "冲进来") and verify result appears without clicking button
- Clear input and verify welcome message reappears
- Rapidly type multiple characters to verify 300ms debounce prevents spam triggers
</verification>

<success_criteria>
1. User can input verb and see results immediately (no Generate button)
2. Search bar stays at top when scrolling with sticky positioning and blur effect
3. Empty state shows welcome message with 4 feature hints
4. 300ms debounce delay prevents excessive search triggers
5. Styling uses design system constants (HEADER_H1, get_card_style, etc.)
</success_criteria>

<output>
Create `.planning/phases/02-verb-content/02-01-SUMMARY.md` when done
</output>
