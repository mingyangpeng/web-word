---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/app.py]
autonomous: true
requirements: [UI-02, UI-03, UI-04]
user_setup: []

must_haves:
  truths:
    - "Verb definitions display in card-based layered layout with four modules"
    - "Semantic decomposition table uses emoji + color coding (blue/green/orange/purple)"
    - "Each card has clickable header for collapse/expand functionality"
    - "Example library has category tabs (方式聚焦类、路径聚焦类、体相聚焦类)"
  artifacts:
    - path: "src/app.py"
      provides: "Card-based results display, semantic table with colors, category tabs"
      min_lines: 120
  key_links:
    - from: "MOCK_DEFINITIONS keys"
      to: "render_verb_result()"
      via: "lookup and display"
      pattern: "MOCK_DEFINITIONS.get(verb,)"
    - from: "card_header()"
      to: "st.session_state.card_expanded"
      via: "toggle logic"
      pattern: "st.session_state.get.*card_expanded"
---

<objective>
Implement card-based results display with semantic decomposition table and example library with category tabs.

Purpose: Provide clear visual hierarchy for academic-style verb definitions with modular, collapsible cards and intuitive navigation through example categories.

Output: Four-module card display, color-coded semantic table, expandable cards, category tab navigation for example library.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-verb-content/02-CONTEXT.md
@.planning/phases/02-verb-content/02-DISCUSSION-LOG.md
@src/design_system.py

# Design System Constants to Use
- HEADER_H2, HEADER_H3: For card titles
- SPACE_MD, SPACE_LG, SPACE_XL: For spacing
- get_card_style(): For card containers
- get_badge_style(color): For category tabs

# Color Coding per CONTEXT.md
- 方式 🏃 → 蓝色 (#4a5568 = PRIMARY_COLOR)
- 路径 🛤️ → 绿色 (#48bb78 = SUCCESS_COLOR)
- 方向 🎯 → 橙色 (#ed8936 = WARNING_COLOR)
- 体相 💪 → 紫色 (#805ad5 = Custom color needed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create semantic table component with emoji color coding</name>
  <files>src/app.py</files>
  <read_first>
    - src/app.py (lines 96-154 for MOCK_DEFINITIONS structure, lines 239-273 for current result display)
    - src/design_system.py (get_table_style, get_table_header_style, get_table_row_style)
  </read_first>
  <behavior>
    - Test 1: Semantic table renders with emoji icons and correct colors
    - Test 2: Table headers use uppercase labels with emoji
    - Test 3: Values are aligned in columns
    - Test 4: Colors match the specification (blue/green/orange/purple)
  </behavior>
  <action>
    1. Add semantic color constants to top of file after design_system imports:
       ```python
       SEMANTIC_COLORS = {
           "方式": {"color": "#4a5568", "emoji": "🏃", "label": "方式 (Manner)"},
           "路径": {"color": "#48bb78", "emoji": "🛤️", "label": "路径 (Path)"},
           "方向": {"color": "#ed8936", "emoji": "🎯", "label": "方向 (Direction)"},
           "体相": {"color": "#805ad5", "emoji": "💪", "label": "体相 (Aspect)"}
       }
       ```
    2. Create `render_semantic_table(semantic_data)` function that:
       - Accepts dict with keys "方式", "路径", "方向", "体相"
       - Returns a styled HTML table with:
         - Each row has emoji icon, semantic label, and description
         - Row background alternates with CARD_COLOR
         - Header row uses PRIMARY_COLOR with white text
         - Table uses get_table_style()

    3. Update MOCK_DEFINITIONS to include semantic decomposition data:
       - Add "semantic_table" key to each verb entry
       - Format as list of lists: [["方式", "跑步", "双脚快速交替蹬地前进"], ...]
       - Example for "跑进来": [["方式", "🏃", "双脚快速交替蹬地前进"], ["路径", "🛤️", "直线穿越边界进入空间"], ["方向", "🎯", "朝向空间内部"], ["体相", "💪", "身体前倾，双臂自然摆动"]]

    4. Create `render_card_header(title, icon, expandable=True)` function:
       - Returns styled HTML for card header
       - Includes toggle arrow icon that changes based on expanded state
       - Uses HEADER_H3 for title
  </action>
  <verify>
    <automated>grep -c "SEMANTIC_COLORS" src/app.py &gt;= 1</automated>
    <automated>grep -c "render_semantic_table" src/app.py &gt;= 1</automated>
    <automated>grep -c "render_card_header" src/app.py &gt;= 1</automated>
    <automated>python -c "import ast; code = open('src/app.py').read(); tree = ast.parse(code); print('Syntax OK')"</automated>
  </verify>
  <done>
    - SEMANTIC_COLORS constant defined with all four semantic types
    - render_semantic_table function created with proper styling
    - MOCK_DEFINITIONS includes semantic_table data for test verbs
    - render_card_header function created for expandable headers
    - Table uses emoji icons and correct colors (blue/green/orange/purple)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement card-based results display with four modules</name>
  <files>src/app.py</files>
  <read_first>
    - src/app.py (current MOCK_DEFINITIONS structure, existing result display at lines 239-273)
  </read_first>
  <behavior>
    - Test 1: Results display in card-based layered layout
    - Test 2: Each module is in its own expandable card
    - Test 3: Cards show/hide on header click
    - Test 4: Four modules are correctly identified
  </behavior>
  <action>
    1. Replace the existing result display (lines 239-273) with new structure:
       - Check if verb exists in MOCK_DEFINITIONS (after adding semantic data)
       - If yes, use MOCK_DEFINITIONS[verb], else use generic example
       - Create `render_verb_result(verb)` function that:
         - Returns semantic_table data from MOCK_DEFINITIONS
         - Returns traditional and optimized definitions

    2. Create four card rendering functions:
       - `render_card_prototype(verb)`: Shows "原型与位移事件分析" header, includes verb and its semantic components
       - `render_card_anchor(verb)`: Shows "范畴锚定" header, explains semantic category
       - `render_card_core(verb)`: Shows "核心释义与多维辨异" header, includes traditional and optimized definitions
       - `render_card_example(verb)`: Shows "图式示例" header, provides illustrative examples

    3. Implement expand/collapse state management in session_state:
       - Use `st.session_state.card_expanded` dict with keys for each card
       - Default to True for all cards
       - Toggle state on header click using st.button with expandable=False

    4. Replace the two-column comparison display with:
       - Four stacked cards using get_card_style()
       - Each card has clickable header
       - Only content is shown when card is expanded
       - Use proper spacing: SPACE_MD between cards

    5. Use HEADER_H2 for section title, HEADER_H3 for card headers
  </action>
  <verify>
    <automated>grep -c "render_card_prototype" src/app.py &gt;= 1</automated>
    <automated>grep -c "render_card_anchor" src/app.py &gt;= 1</automated>
    <automated>grep -c "render_card_core" src/app.py &gt;= 1</automated>
    <automated>grep -c "render_card_example" src/app.py &gt;= 1</automated>
    <automated>grep -c "st.session_state.card_expanded" src/app.py &gt;= 1</automated>
    <automated>python -c "import ast; code = open('src/app.py').read(); tree = ast.parse(code); print('Syntax OK')"</automated>
  </verify>
  <done>
    - Four card functions (prototype, anchor, core, example) implemented
    - Session state manages expand/collapse for each card
    - Cards display in layered layout with proper spacing
    - Headers are clickable and show expand/collapse indicators
    - Default state shows all cards expanded
  </done>
</task>

<task type="auto">
  <name>Task 3: Create example library with category tabs</name>
  <files>src/app.py</files>
  <read_first>
    - src/app.py (current sidebar and any existing tab components)
    - src/design_system.py (get_badge_style for tab styling)
  </read_first>
  <behavior>
    - Test 1: Three category tabs are displayed (方式聚焦类、路径聚焦类、体相聚焦类)
    - Test 2: Clicking a tab switches to that category
    - Test 3: Each tab shows relevant verbs
    - Test 4: Tab styling uses badge style with color
  </behavior>
  <action>
    1. Define example library categories after MOCK_HOT_VERBS:
       ```python
       CATEGORY_MODE = "方式聚焦类"
       CATEGORY_PATH = "路径聚焦类"
       CATEGORY_MANNER = "体相聚焦类"

       EXAMPLE_VERBS = {
           CATEGORY_MODE: ["跑进来", "冲进来", "滑进来", "滚进来", "跌进来", "窜进来"],
           CATEGORY_PATH: ["走进去", "走进来", "冲出去", "滚出去", "跌出去", "滑出去"],
           CATEGORY_MANNER: ["站着", "躺着", "坐着", "蹲着", "趴着", "靠着"]
       }
       ```

    2. Create `render_example_library()` function that:
       - Uses st.tabs() with three tab keys: "方式聚焦类", "路径聚焦类", "体相聚焦类"
       - For each tab, displays verb buttons using get_badge_style()
       - Each button triggers search with that verb

    3. Add example library section after result display:
       - Check if st.session_state.show_result is True
       - If yes, show example library with tabs
       - Use get_section_spacing() before adding section

    4. Style tabs using get_badge_style() for active states
  </action>
  <verify>
    <automated>grep -c "EXAMPLE_VERBS" src/app.py &gt;= 1</automated>
    <automated>grep -c "render_example_library" src/app.py &gt;= 1</automated>
    <automated>grep -c "CATEGORY_MODE\|CATEGORY_PATH\|CATEGORY_MANNER" src/app.py &gt;= 1</automated>
    <automated>python -c "import ast; code = open('src/app.py').read(); tree = ast.parse(code); print('Syntax OK')"</automated>
  </verify>
  <done>
    - Three category tabs defined (方式聚焦类、路径聚焦类、体相聚焦类)
    - render_example_library function creates tab interface
    - Each tab displays relevant verb buttons
    - Clicking a button triggers search for that verb
    - Tabs styled with badge-style colors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Result Display | User clicks verb from example library → triggers search |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Injection | EXAMPLE_VERBS data | accept | Static data, no user input involved |
| T-02-04 | Tampering | St.session_state.card_expanded | accept | Local session state, no persistence |
</threat_model>

<verification>
[Phase 2 UI-02/UI-03/UI-04 Verification]
- Run Streamlit app: streamlit run src/app.py
- Search for "跑进来" and verify four-module card display appears
- Click each card header to verify collapse/expand functionality
- Check semantic table has emoji icons (🏃🛤️🎯💪) and correct colors
- Switch between example library tabs to verify category switching
- Click a verb button from example library and verify search triggers
</verification>

<success_criteria>
1. Results display in four separate cards (prototype, anchor, core, example)
2. Each card has clickable header with expand/collapse indicator
3. Semantic table uses emoji icons and color coding (blue/green/orange/purple)
4. Example library has three category tabs with verb buttons
5. Clicking example library verbs triggers search immediately
6. Styling uses design system constants throughout
</success_criteria>

<output>
Create `.planning/phases/02-verb-content/02-02-SUMMARY.md` when done
</output>
