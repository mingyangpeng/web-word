---
phase: 01-verb-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/design_system.py, src/app.py, docs/design-system.md]
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-03]
user_setup: []

must_haves:
  truths:
    - "所有颜色值集中定义在设计系统模块中"
    - "所有字体和间距值有统一的常量定义"
    - "创建了一个可复用组件函数库（按钮、卡片、输入框）"
    - "响应式布局在桌面端 Chrome 浏览器下正常工作"
    - "文字对比度符合 WCAG AA 标准（基础检查）"
  artifacts:
    - path: "src/design_system.py"
      provides: "设计系统核心（颜色、字体、间距、组件样式函数）"
      min_lines: 200
    - path: "src/app.py"
      provides: "应用代码引用设计系统常量和函数"
      modified_lines: [1-20, 188-418]
    - path: "docs/design-system.md"
      provides: "设计系统文档"
      min_lines: 100
  key_links:
    - from: "src/app.py"
      to: "src/design_system.py"
      via: "import and use constants/functions"
      pattern: "from src import design_system"
---

<objective>
建立统一的视觉设计系统，为 Streamlit 应用提供一致的设计规范。

**Purpose:** 通过集中式颜色、字体、间距和组件样式定义，提高代码可维护性，减少视觉不一致，为后续阶段所有 UI 改进提供统一基础。

**Output:** 设计系统模块、样式指南文档、重构后的应用代码
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verb-tool/01-CONTEXT.md
@.planning/phases/01-verb-tool/01-RESEARCH.md
@src/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design system module</name>
  <files>src/design_system.py</files>
  <action>
Create the centralized design system module per RESEARCH.md patterns. This file will contain all design constants and component style functions.

Key requirements:
1. Define color system with 10+ color constants based on user decision (white + blue-gray scheme)
   - Primary: #4a5568 (blue-gray)
   - Secondary: #63b3ed (light blue)
   - Success/Error/Warning: green/red/orange
   - Background/Card/Text/Border: white/light gray/dark gray
2. Define spacing system based on 8px unit (SPACE_XS=8, SPACE_SM=16, SPACE_MD=24, SPACE_LG=32, SPACE_XL=48)
3. Define font system (HEADER_H1-4, BODY_SM/BASE/LG, LINE_HEIGHT_* constants)
4. Create component style functions:
   - get_button_primary_style()
   - get_button_secondary_style()
   - get_card_style()
   - get_input_field_style()
   - get_input_field_focused_style()
   - get_container_style(max_width=1200)
   - get_table_style(), get_table_header_style(), get_table_row_style(), get_table_cell_style()
5. Add utility function check_contrast() to validate WCAG AA compliance for all colors against background/text
6. Include comprehensive docstrings for each constant and function

Do NOT:
- Create separate Markdown documentation yet (that's Task 2)
- Implement complex animations or effects (keep simple transitions)
- Define theme switching logic (per user decision: fixed light theme)
- Include mock data or business logic
</action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'src'); from design_system import *; print('✓ design_system.py imports successfully'); print(f'✓ PRIMARY_COLOR={PRIMARY_COLOR}'); print(f'✓ check_contrast(PRIMARY_COLOR, BACKGROUND_COLOR)={[ok for ok, _ in [(check_contrast(PRIMARY_COLOR, BACKGROUND_COLOR), check_contrast(PRIMARY_COLOR, BACKGROUND_COLOR))][0]]}')" 2>&1 | head -20</automated>
  </verify>
  <done>
    design_system.py exists with 200+ lines containing:
    - 10+ color constants (PRIMARY, SECONDARY, SUCCESS, ERROR, WARNING, BACKGROUND, CARD, TEXT, TEXT_SECONDARY, BORDER)
    - 5+ spacing constants (SPACE_XS to SPACE_XL)
    - 6+ font constants (HEADER_H1-4, BODY_SM/BASE/LG)
    - 7+ component style functions
    - 1 contrast check utility function
    - All functions return valid CSS strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create design system documentation</name>
  <files>docs/design-system.md</files>
  <action>
Create comprehensive design system documentation in Markdown format.

Structure:
1. Title and overview (简体中文)
2. Color system section with color swatches (hex codes, visual preview, WCAG AA contrast ratios)
3. Font system section (font sizes, line heights, usage guidelines)
4. Spacing system section (8px base unit, scale chart, usage examples)
5. Component style guide with code examples for each component type
6. Usage guidelines (when to use each component, customization notes)
7. Accessibility notes (WCAG AA compliance, keyboard navigation support)
8. Revision history section

Content requirements:
- Include actual color hex codes and calculated contrast ratios from design_system.py
- Provide visual swatches using CSS colored boxes or markdown color syntax
- Document all constants from design_system.py
- Include usage examples for each component function
- Reference WCAG 2.1 AA standard (contrast >= 4.5:1 for normal text)
- Keep language in Chinese (匹配项目定位)

Do NOT:
- Include implementation code (code goes in design_system.py)
- Include navigation or links to other documents
- Create separate accessibility guide yet (DESIGN-03 is handled in execution verification)
</action>
  <verify>
    <automated>grep -c "颜色系统\|Font\|间距" docs/design-system.md && grep -c "#" docs/design-system.md && wc -l docs/design-system.md</automated>
  </verify>
  <done>
    design-system.md exists with:
    - Color system section with visual swatches and contrast ratios
    - Font system section with size guidelines
    - Spacing system section with 8px base unit
    - Component style guide with examples
    - Usage guidelines section
    - Accessibility notes section
    - At least 100 lines of documentation
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor app.py to use design system</name>
  <files>src/app.py</files>
  <action>
Refactor app.py to use the new design system module, replacing hardcoded values.

Implementation steps:
1. Add import: `from src.design_system import *` (or appropriate path)
2. Replace hardcoded colors with design system constants:
   - Remove inline CSS magic numbers for colors
   - Use PRIMARY_COLOR, TEXT_COLOR, BORDER_COLOR, etc.
3. Update component style functions to use design system:
   - Replace get_button_style() call with get_button_primary_style()
   - Replace card styles with get_card_style()
   - Replace input field styles with get_input_field_style()
   - Use get_container_style() for main container
   - Use get_table_*_style() functions for any tables
4. Update hardcoded spacing values to use SPACE_* constants
5. Update hardcoded font sizes to use HEADER_* and BODY_* constants
6. Keep existing inline CSS for sticky search bar (special effect, not covered by design system)
7. Maintain existing functionality - no logic changes, only visual refactoring

Do NOT:
- Change any business logic (data handling, API calls, etc.)
- Modify the sticky search bar CSS (that's a special effect requiring inline CSS)
- Add new features or components
- Change the text content or user interactions
- Remove or replace any existing functional code
</action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'src'); import app; print('✓ app.py imports successfully'); from design_system import *; print('✓ Design constants available:', len(['PRIMARY_COLOR', 'SECONDARY_COLOR', 'SUCCESS_COLOR', 'ERROR_COLOR', 'WARNING_COLOR', 'BACKGROUND_COLOR', 'CARD_COLOR', 'TEXT_COLOR', 'TEXT_SECONDARY', 'BORDER_COLOR', 'SPACE_XS', 'SPACE_SM', 'SPACE_MD', 'SPACE_LG', 'SPACE_XL', 'HEADER_H1', 'HEADER_H2', 'HEADER_H3', 'HEADER_H4', 'BODY_SM', 'BODY_BASE', 'BODY_LG', 'LINE_HEIGHT_BASE', 'check_contrast']))"</automated>
  </verify>
  <done>
    app.py successfully refactored with:
    - Design system imported at top
    - All hardcoded colors replaced with design_system constants
    - Component style calls use design_system functions
    - Spacing values replaced with SPACE_* constants
    - Font sizes replaced with HEADER_* and BODY_* constants
    - All functionality preserved (no logic changes)
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify responsive layout and accessibility</name>
  <files>src/app.py, docs/design-system.md</files>
  <action>
Verify that the design system implementation meets responsive and accessibility requirements.

Testing checklist:
1. Responsive layout verification:
   - Run app in Chrome desktop browser at 1920px width (two-column layout should be visible)
   - Run app at 1366px width (should still show two columns or gracefully stack)
   - Test at 768px width (columns should stack as single column)
   - Test at 375px width (single column layout, readable text)
   - Verify Streamlit's st.columns() works correctly with design system styles

2. Accessibility verification:
   - Use browser DevTools Accessibility panel (F12 → Accessibility)
   - Check all interactive elements have appropriate labels
   - Verify Tab key navigation works through all buttons and inputs
   - Test color contrast ratios for all text against backgrounds:
     - Text (TEXT_COLOR #1a202c) on white (BACKGROUND_COLOR #ffffff): expect > 7:1
     - Primary button text on PRIMARY_COLOR #4a5568: expect > 4.5:1
     - Secondary text on white: expect > 7:1
   - Run contrast check via utility function in design_system.py
   - Ensure no pure decorative images without alt text (Streamlit uses emojis/icons mostly)

3. Documentation verification:
   - Confirm docs/design-system.md includes accessibility notes
   - Verify color contrast ratios are documented
   - Check WCAG AA standard reference is included

4. Visual consistency check:
   - Open app and verify all colors match design system definitions
   - Check spacing is consistent (8px grid)
   - Verify font sizes are appropriate
   - Confirm button/card styles look as designed

Create a simple verification report in comments within app.py or in a temporary file.

Do NOT:
- Fix accessibility issues beyond basic verification (will be addressed in DESIGN-03 tasks if needed)
- Modify responsive behavior (Streamlit's built-in behavior is acceptable)
- Change any colors after verification (design system is now locked)
</action>
  <verify>
    <automated>python -c "
import sys
sys.path.insert(0, 'src')
from design_system import *

# Verify all color pairs meet WCAG AA (contrast >= 4.5:1)
test_pairs = [
    (TEXT_COLOR, BACKGROUND_COLOR),
    (PRIMARY_COLOR, BACKGROUND_COLOR),
    (PRIMARY_COLOR, TEXT_COLOR),
    (TEXT_SECONDARY, BACKGROUND_COLOR),
]

print('WCAG AA Contrast Check Results:')
print('=' * 50)
for color1, color2 in test_pairs:
    ok, ratio = check_contrast(color1, color2)
    status = 'PASS' if ok else 'FAIL'
    print(f'{color1} on {color2}: {ratio:.2f}:1 {status}')
print('=' * 50)
"</automated>
  </verify>
  <done>
    Responsive layout verified working in:
    - 1920px (two-column layout)
    - 1366px (two-column layout)
    - 768px (single-column stack)
    - 375px (single-column layout, readable)
    Accessibility verified:
    - All interactive elements have labels
    - Tab key navigation works
    - Color contrast ratios meet WCAG AA (>= 4.5:1)
    - docs/design-system.md includes accessibility notes
    - Visual consistency confirmed (all colors/spacing/fonts match design system)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.py → browser | Untrusted input crosses here (user text input, no injection risk in Python code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Tampering | design_system.py | accept | No external data access, constants only |
| T-02 | Information Disclosure | design_system.py | mitigate | Add docstrings, avoid hardcoding secrets |
| T-03 | Injection | app.py (inline CSS) | accept | CSS injection uses design system constants, not user input |
</threat_model>

<verification>
Overall phase verification:
1. Design system module created with all required constants and functions
2. Documentation created with comprehensive guidelines
3. app.py refactored to use design system (no logic changes)
4. Responsive layout verified across screen sizes
5. Accessibility (WCAG AA) verified for color contrast
6. Visual consistency confirmed across all UI elements

Gate: All tasks pass verification criteria, design system is complete and locked for future phases
</verification>

<success_criteria>
1. design_system.py contains 200+ lines with complete color/font/spacing system
2. docs/design-system.md documents all design guidelines in Chinese
3. app.py uses design system constants instead of hardcoded values
4. Responsive layout works at 1920px, 1366px, 768px, 375px widths
5. Color contrast ratios meet WCAG AA standard (>= 4.5:1)
6. Visual consistency verified across all UI elements
</success_criteria>

<output>
Create `.planning/phases/01-verb-tool/01-01-SUMMARY.md` when done
</output>
